% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmip6.R
\name{cmip6}
\alias{cmip6}
\title{Query CMIP6 catalog}
\usage{
cmip6(
  project_id = "CMIP",
  member_id = "r1i1p1f1",
  realm = NULL,
  frequency = NULL,
  table_id = NULL,
  institution_id = NULL,
  source_id = NULL,
  experiment_id = NULL,
  variable_id = NULL,
  grid_label = NULL,
  version = NULL
)
}
\arguments{
\item{project_id}{Character filter for the \code{project_id} column. Defaults to "CMIP".}

\item{member_id}{Character filter for the \code{member_id} column. Defaults to "r1i1p1f1".}

\item{realm}{Character filter for the \code{realm} column (e.g. "atmos").}

\item{frequency}{Character filter for the \code{frequency} column (e.g. "mon").}

\item{table_id}{Character filter for the \code{table_id} column (e.g. "Amon").}

\item{institution_id}{Character filter for the \code{institution_id} column.}

\item{source_id}{Character filter for the \code{source_id} column.}

\item{experiment_id}{Character filter for the \code{experiment_id} column.}

\item{variable_id}{Character filter for the \code{variable_id} column (e.g. "tas").}

\item{grid_label}{Character filter for the \code{grid_label} column.}

\item{version}{Character filter for the \code{version} column.}
}
\value{
A \code{data.table} with rows from the \code{catalog} table matching the
supplied filters. Only non-NULL arguments are used as filters.
}
\description{
Query a local CMIP6 catalog database built from CSV catalog files and
return catalog rows matching the provided filters as a data.table.
}
\details{
The function uses a local SQLite database derived from
the CSV catalog files.
Database rebuild is moderately slow, but it only needs to be done once unless
the original catalogues are updated.
}
\examples{
cmip6(frequency = "mon", variable_id = "tas")


}
